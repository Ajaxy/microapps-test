export default {
    DONATION_DELAY: 60 * 60 * 1000, // 60 min.
    DONATION_DELAY_INTERVAL: 333,
    MIN_AMOUNT: 1,
    MAX_AMOUNT: 100,
    PAYON_RESULT_URL: location.href.replace(location.search, '')
};